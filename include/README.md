This package does not publish vendor C headers.

The vendor `SDK/inc/spectrometer.h` file is the internal ABI source of truth,
but it is not included in the generated public branch unless redistribution is
explicitly approved.
